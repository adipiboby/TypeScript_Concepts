{
  "compilerOptions": {
    "target": "es6",
    "removeComments": true,
    "noEmitOnError": true,
    "pretty": true,
    "outDir": "./dist",
    "rootDir": "./src",
  //  "strictNullChecks": true,
  //  "noImplicitAny": true  //no variable should not have any, that means not accepting every datatype only what we define when creating variable
  }
}
